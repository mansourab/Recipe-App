<div class="col-md-6 m-auto">
    {{ form_start(form) }}

        {{ form_row(form.title) }}

        {{ form_row(form.content) }}

        {{ form_row(form.categories) }}

        {{ form_row(form.activeTime) }}

        {{ form_row(form.totalTime) }}



        <button class="btn btn-primary mt-2 btn-lg btn-block">{{ button_label|default('Enregistrer') }}</button>
    {{ form_end(form) }}
</div>
